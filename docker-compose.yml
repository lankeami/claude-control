services:
  server:
    build: ./server
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - claude-controller-data:/data
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    restart: unless-stopped

volumes:
  claude-controller-data:
