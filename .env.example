# Claude Controller configuration
# Copy to .env and fill in your values

# Server port
PORT=8080

# Claude config directory (where settings.json lives)
CLAUDE_DIR=~/.claude

# ngrok auth token (optional, for remote access)
NGROK_AUTHTOKEN=
